{
  "description": "MCP SSH Bridge plugin hooks",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "echo 'MCP SSH Bridge plugin loaded. Use /bridge-mcp:bridge to manage remote hosts, or /bridge-mcp:discover to explore 338 tools.'"
          }
        ]
      }
    ]
  }
}
